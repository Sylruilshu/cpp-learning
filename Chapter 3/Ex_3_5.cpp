// the results will not be the same and they shouldnt be as ints can only hold whole number integers which means all results will be whole number integers, whereas
// with doubles you are able to use floating point numbers, this implies the results may also be floating point numbers.